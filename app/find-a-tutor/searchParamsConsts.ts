export const subject = "subject";
export const level = "level";

export const isSubjectSelected = (value: string | null): boolean => {
  return value !== null && value !== "";
};

export const isLevelSelected = (value: string | null): boolean => {
  return value !== null && value !== "";
};
